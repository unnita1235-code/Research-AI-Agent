version: "3.9"

services:
  research-agent:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - research_data:/app/data

volumes:
  research_data:
